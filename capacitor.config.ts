import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Miestacionamiento_fw_2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
